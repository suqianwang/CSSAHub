<table>
  <tr>
    <th>Ride ID</th>
    <th>Type</th>
  </tr>
  <% @rides.each do |ride| %>
    <tr>
      <td><%= ride.id %></td>
      <td><%= ride.role %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to 'Create Ride', new_ride_url %>